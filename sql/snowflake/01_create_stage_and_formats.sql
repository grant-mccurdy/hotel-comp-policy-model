USE ROLE HOTEL_COMP_DEV_ROLE;
USE WAREHOUSE HOTEL_COMP_WH;
USE DATABASE HOTEL_COMP_POLICY_MODEL;

CREATE SCHEMA IF NOT EXISTS RAW;
CREATE SCHEMA IF NOT EXISTS STAGING;
CREATE SCHEMA IF NOT EXISTS MARTS;
CREATE SCHEMA IF NOT EXISTS AUDIT;

CREATE OR REPLACE FILE FORMAT RAW.CSV_WITH_HEADER
  TYPE = CSV
  SKIP_HEADER = 1
  FIELD_OPTIONALLY_ENCLOSED_BY = '"'
  EMPTY_FIELD_AS_NULL = FALSE
  NULL_IF = ();

CREATE STAGE IF NOT EXISTS RAW.PROJECT_CSV_STAGE
  FILE_FORMAT = RAW.CSV_WITH_HEADER
  COMMENT = 'Internal stage for public-safe CSV artifacts generated by this repository.';
