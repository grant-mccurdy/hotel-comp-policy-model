[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "hotel-comp-policy-model"
version = "0.1.0"
description = "Explainable hotel service-recovery policy and decision runtime"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
include = ["scripts*"]
